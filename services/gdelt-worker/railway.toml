[build]
builder = "NIXPACKS"

# Do not set a shared startCommand here.
# Railway detects this file for every service using /services/gdelt-worker as root,
# so per-service commands must be configured in the Railway UI.
